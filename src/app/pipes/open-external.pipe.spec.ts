import { OpenExternalPipe } from './open-external.pipe';

describe('OpenExternalPipe', () => {
  it('create an instance', () => {
    const pipe = new OpenExternalPipe();
    expect(pipe).toBeTruthy();
  });
});
